export enum BrtFieldType {
  TEXT = 'text',
  TEXTAREA = 'textarea',
  EMAIL = 'email',
  CHECKBOX = 'checkbox',
  RATING = 'rating',
  SELECT = 'select'
}

// impact brtField interface if you edit this enum
